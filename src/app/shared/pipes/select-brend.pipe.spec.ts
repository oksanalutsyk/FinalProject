import { SelectBrendPipe } from './select-brend.pipe';

describe('SelectBrendPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectBrendPipe();
    expect(pipe).toBeTruthy();
  });
});
