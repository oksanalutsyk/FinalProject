import { SelectCategoryPipe } from './select-category.pipe';

describe('SelectCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
