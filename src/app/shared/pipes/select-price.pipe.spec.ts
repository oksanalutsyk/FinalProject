import { SelectPricePipe } from './select-price.pipe';

describe('SelectPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectPricePipe();
    expect(pipe).toBeTruthy();
  });
});
