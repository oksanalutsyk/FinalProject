import { SelectSizePipe } from './select-size.pipe';

describe('SelectSizePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectSizePipe();
    expect(pipe).toBeTruthy();
  });
});
